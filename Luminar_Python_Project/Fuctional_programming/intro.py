# Functional Programming
# lambda
# map
# filter
# list comprehension
#
# 1)lambda---->anonymous function
#     syntax:
#             var=lambda arguments:operations
# 2)Map
# Used in a list for getting an output corresponding to all the elements in a list
# Syntax:
#           var=list(map(function,iterable))
# 3)Filter
# Used in list where we have to filter based on a condition
# Syntax:
#             var=list(filter(function,iterable))
